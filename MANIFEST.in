include README.md
include LICENSE
include Makefile
include setup.py
include tact/bin/.gitkeep
recursive-include native *.c *.h
# build artifact — sdist consumers rebuild it via make; the wheel gets it from build_py
exclude tact/bin/libtact.so
